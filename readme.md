A Node.js + Express "Student Batch API",

A visible HTML page at / (so you can hit the IP in a browser and see it live)
A /health endpoint - checks the health of server
One real API route like /api/batches returning dummy JSON


"CI/CD Implemented!"

<!-- GITSCM TESTING -->